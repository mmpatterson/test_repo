

# git status
    #checks difference between local repo and github repo
# git clone 
    #matches what is in github
# git add .
    #gathers files for build (. indicates everything below current folder)
# git commit -m "" (need message during commmit)
# git push origin master (pushes files to github)