

# git status
    #checks difference between local repo and github repo
# git clone 
    #matches what is in github
# git add .
    #gathers files for build 
    # The '.' indicates everything below current folder)
# git commit -m "" 
    # (need message during commmit)
# git push origin master (pushes files to github)

# git reset --hard origin master

#Add Branch
#git checkout -b "branch name"

#Push from local to branch
# git push --set-upstream origin branch_name